---
title: "Integrate with Zephyr"
description: "Sync QA Wolf automated test results to Zephyr Test Management for Jira, including test cycles and metadata."
sidebarTitle: "Zephyr"
---

## What QA Wolf syncs

For each QA Wolf run, the integration can:

- Create test executions in Zephyr.
- Associate executions with a test cycle.
- Sync automated test results (passed, failed, skipped).
- Include execution metadata (cycle name, environment).

Results are synced after runs reach Completed status in QA Wolf.

## Limitations

- Cloud-hosted only (Jira Cloud with Zephyr Squad or Scale).
- Only one Zephyr-enabled Jira project can be configured per QA Wolf workspace.
- Zephyr test cases must already exist.
- Custom fields are not supported unless specially configured.

## Requirements

- Jira Cloud instance
- Zephyr installed and licensed (Squad or Scale)
- API access enabled via the Atlassian REST API, which requires an Atlassian account administrator

## How to set up the integration

<Steps>
  <Step title="Set up Jira and Zephyr">
    Enable Zephyr on the Jira project that will receive test executions, and create the Zephyr test cases QA Wolf will link results to. Tests are not created automatically.
  </Step>

  <Step title="Configure QA Wolf">
    Provide your Jira user email, API token, and base URL, the Zephyr project key (for example, `QA` or `MOBILE`), and optionally a default test cycle name or naming convention.
  </Step>

  <Step title="Verify the integration">
    Once QA Wolf enables the integration, run your automated tests. Test executions appear in the configured Zephyr project, grouped under the appropriate test cycle.

    If executions don't appear as expected, or test cases aren't mapping correctly, contact your QA Wolf representative.
  </Step>
</Steps>
