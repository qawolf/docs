---
title: "Connect your agent"
sidebarTitle: "Preview: iframe"
description: "Embed preview — the Connect your agent flow rendered in an iframe."
mode: "wide"
---

{/*
  PREVIEW PAGE — deliberately blank apart from the embed, so the page shows
  what the docs shell looks like when this is the only content on it.

  Approach A: iframe the existing prototype at qaw-prototype.vercel.app.
  Verified embeddable — that host sends no X-Frame-Options and no CSP
  frame-ancestors directive, and docs.qawolf.com's enforced CSP sets no
  frame-src.

  allow="clipboard-write" is required: the panel's Copy buttons call
  navigator.clipboard, which is gated in a cross-origin frame without it.

  Sizing lives in /style.css, not in a <style> tag here — Mintlify strips
  inline <style> from MDX, and it injects an inline aspect-ratio onto every
  iframe that a class rule has to override. See the comments there.
*/}

<div className="qaw-embed-wrap">
  <iframe
    className="qaw-embed-frame"
    src="https://qaw-prototype.vercel.app/connect-agent"
    title="Connect your agent"
    allow="clipboard-write"
  />
</div>
